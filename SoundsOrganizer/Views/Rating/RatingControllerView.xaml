<UserControl x:Class="SoundsOrganizer.Views.Rating.RatingControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="59"
             DataContext="{Binding Path=RatingControllerViewModel, Source={StaticResource ViewModelLocator}}" Width="44.842">
    
    <Grid>
        <Button VerticalAlignment="Top" Height="22" Command="{Binding SaveCommand}" >Save</Button>
    </Grid>

</UserControl>
